Function {
    name: "main",
    chunk: [
        function(fib(n)),
        global_var_declaration(fib),
        read_global_variable(fib),
        constant(5),
        call(1),
        print,
    ],
}